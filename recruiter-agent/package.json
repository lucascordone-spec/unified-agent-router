{
  "name": "recruiter-agent",
  "version": "1.0.0",
  "description": "Autonomous agent that finds DeFi AI agents and pitches Unified Agent Router integration",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "run": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
